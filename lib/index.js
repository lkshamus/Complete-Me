module.exports = class Node {
  constructor() {
    this.root = null;
    this.words = 0;
    this.letters = 0;
    this.children = {};
  }
  insert(word) {
    if(!this.root){
      this.root = new Node(data);
    };
    let letter = word.charAt(0);
  }
  count(){

  }
}